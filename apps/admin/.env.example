VITE_ADMIN_DATA_SOURCE=fixture
VITE_API_URL=http://localhost:8787
VITE_DEV_USER_ID=usr-admin-1

# Production Studio website shortcut: staging until the approved main-site launch.
# Set live only for the final mukhtalif.net cutover. Development always stays on its Worker.
VITE_PRODUCTION_WEB_TARGET=staging

# Add these when Supabase Auth is enabled in the admin frontend.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
