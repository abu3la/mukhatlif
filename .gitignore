node_modules/

# builds
dist/
build/
.next/
# Regenerated by `next build` / `next dev`; it references paths under .next/,
# so committing it would break type-check on a fresh clone.
next-env.d.ts
.open-next/
.expo/
*.tsbuildinfo

# tooling caches
.turbo/
.wrangler/

# local secrets
.env
.env.*
!.env.example
.dev.vars
!.dev.vars.example

# os
.DS_Store

# graphify knowledge graph (rebuildable)
graphify-out/
